#include <stdio.h>

#define MAXLEN 20

double atof(char s[]);

/* This is the solution to the K&R C 4-2. */
int main()
{
	char str[MAXLEN];

	scanf("%s", str);
	printf("%s\n%g\n", str, atof(str));

	return 0;
}



